/*#include "linker.h"

void minimum(void)
{
	size_t argc = sizeof(intArr)/sizeof(intArr[0]);
	double min = intArr[0];

	for(int i = 0; i < argc; i++)
	{
		if(min > intArr[i])
			min = intArr[i];
	}

	printf("Max: ");
	printf("%f\n", min);

}*/
