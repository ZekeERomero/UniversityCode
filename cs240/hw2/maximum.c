/*#include "linker.h"

void maximum(void)
{
	size_t argc = sizeof(intArr)/sizeof(intArr[0]);
	double max = intArr[0];

	for(int i = 0; i < argc; i++)
	{
		if(max < intArr[i])
			max = intArr[i];
	}

	printf("Max: ");
	printf("%f\n", max);

}*/
